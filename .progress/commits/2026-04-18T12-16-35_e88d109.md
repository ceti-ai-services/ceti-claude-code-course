# e88d109 — feat: full course, stripe payment links, admin bypass, $1k retainer

- **Date**: 2026-04-18T12:16:35-06:00
- **Author**: Manutej Mulaveesala <manutej@gmail.com>
- **Branch**: main
- **Type**: feat
- **Scale**: 15 files changed, +11,357 / -180

## What it does

Major content + payments milestone:

- **Dynamic lesson route**: new `app/pages/start/[slug].vue` renders all 7 Bronze lessons from `content/start/*.md` with prev/next nav + progress dots. Removed the one-off `01-mental-model.vue` in favor of the dynamic path.
- **Tier-page rewrite**: Silver and Gold pages now have substantive content — week-by-week curriculum, personas, takeaways/deliverables, FAQ, retainer callout. Addresses feedback that tier pages felt empty.
- **Pricing update**: retainer changed `$500/mo → $1,000/mo` across landing, Gold page, and `CLAUDE.md`.
- **Stripe Payment Links**: minimal setup, zero secret keys in code. See `docs/STRIPE-SETUP.md` for the 5-minute flow.
- **Admin bypass via `ADMIN_KEY` (`MERCURIOP0656`)**:
  - `/api/enroll` accepts it as a valid Bronze code
  - `/api/checkout/{silver,gold}` accepts `adminKey` in body → skips Stripe
  - Silver/Gold pages read `?admin=<key>` from URL and forward it
  - `/thanks` shows admin-test banner when `?admin=1` present
- **Preset removed** (`preset: 'static'` deleted) so `/api` runs as Vercel serverless functions
- Added `marked` + `gray-matter` for markdown rendering

## Why it matters

Site is now a working end-to-end funnel — content reads, payment paths exist, admin can dry-run every flow without paying.
