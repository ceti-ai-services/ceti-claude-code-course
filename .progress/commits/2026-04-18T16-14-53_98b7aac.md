# 98b7aac — docs: autonomous 4-hour build runbook — interactive-first rewrite

- **Date**: 2026-04-18T16:14:53-06:00
- **Author**: Manutej Mulaveesala <manutej@gmail.com>
- **Branch**: main
- **Type**: docs
- **Files**: `docs/AUTONOMOUS-BUILD.md` (+636, new)

## What it does

Replaces the prior runbook with a version that matches the `agentic-ai-course` / `raven-cargo-course` gold standard: high interactivity, low prose density, canvas-led hero, motion reinforces pedagogy. Previous version was markdown-heavy and would have reproduced the "too text-heavy" problem.

## Key shifts

- **Package 0** is now a study phase producing `docs/GOLD-STANDARD.md`.
- **Package 1** is a 14-component Vue library (HeroCanvas, ModuleHero, Quiz, StepWalk, ClickCards, Callout, IxCollapse, …).
- **Package 3** rebuilds Bronze 01 as the reference interactive lesson.
- **Density rule**: max 3–4 prose lines before a visual break.
- **Motion curve**: same `cubic-bezier(0.22, 1, 0.36, 1)` used in the reference.
- Source markdown preserved as **input**, not output.

Includes state detection, per-package protocol, 6 quality gates, HANDOFF.md protocol, failure-mode recovery, and a token budget.

## Why it matters

This is the meta-prompt the parallel build agent is currently executing. Every subsequent commit on `main` should map to a package in this runbook.
